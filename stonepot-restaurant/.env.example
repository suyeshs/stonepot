# ============================================================================
# Stonepot Restaurant - Environment Variables
# ============================================================================
#
# CREDENTIALS:
# - Credentials are stored in the root /credentials/ directory
# - See /credentials/README.md for setup instructions
# - The shared config (/config/shared.js) automatically loads credentials
#
# SHARED CONFIG:
# - Common variables can be set in /config/shared.env.example
# - Project-specific overrides go in this file
# ============================================================================

# Vertex AI Configuration (optional - uses shared config if not set)
VERTEX_PROJECT_ID=your-gcp-project-id
VERTEX_LOCATION=us-central1
VERTEX_MODEL_ID=gemini-2.0-flash-exp

# Firebase Configuration (optional - uses shared config if not set)
FIREBASE_PROJECT_ID=your-firebase-project

# Cloudflare Display API
CLOUDFLARE_WORKER_URL=https://your-worker.workers.dev
CLOUDFLARE_AUTH_TOKEN=your-secret-token-here

# Server Configuration
PORT=3001
NODE_ENV=development
HOST=0.0.0.0

# CORS
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Session Configuration
SESSION_TIMEOUT=600000
MAX_CONVERSATION_HISTORY=50

# Logging
LOG_LEVEL=info
