# Shared Environment Variables
# Copy this to the root as .env or to individual projects

# ============================================================================
# Google Cloud / Firebase Configuration
# ============================================================================

# Your Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Firebase Project ID (usually same as GCP project)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase Database URL (for Realtime Database, if used)
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# ============================================================================
# Vertex AI Configuration
# ============================================================================

# Vertex AI Project ID (usually same as GOOGLE_CLOUD_PROJECT)
VERTEX_PROJECT_ID=your-gcp-project-id

# Vertex AI Location/Region
VERTEX_LOCATION=us-central1

# Vertex AI Model ID
VERTEX_MODEL_ID=gemini-2.0-flash-exp

# ============================================================================
# Cloudflare Configuration (for restaurant-display)
# ============================================================================

# Cloudflare Account ID
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# Cloudflare API Token (optional, can use credentials/cloudflare-api-token.txt)
CLOUDFLARE_API_TOKEN=your-api-token

# Cloudflare Worker URL (after deployment)
CLOUDFLARE_WORKER_URL=https://your-worker.workers.dev

# ============================================================================
# Application Configuration
# ============================================================================

# Environment (development, staging, production)
NODE_ENV=development

# Logging level (debug, info, warn, error)
LOG_LEVEL=info

# ============================================================================
# CORS Configuration
# ============================================================================

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com

# ============================================================================
# Notes
# ============================================================================
#
# Credential Files:
# - The actual credential JSON files should be placed in /credentials/
# - See /credentials/README.md for detailed instructions
#
# Project-Specific Variables:
# - Each project may have additional variables in their own .env files
# - See each project's .env.example for project-specific configuration
#
# Security:
# - Never commit .env files to version control
# - Keep credentials secure and rotate regularly
# - Use different credentials for production and development
